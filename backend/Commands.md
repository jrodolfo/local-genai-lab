That's expected — there's no root / endpoint. All endpoints are under /api/. Try these in your browser:

- http://localhost:8081/api/models — list available models
- http://localhost:8081/api/chat — chat endpoint
- http://localhost:8081/api/sessions — list sessions
- http://localhost:8081/api/rag/status — RAG status
- http://localhost:8081/actuator — actuator info
